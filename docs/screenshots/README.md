# Screenshots

Real-run screenshots for the main README (CLAUDE.md §5 — captured from an actual
session against the real example document, never staged).

Save the four images here with these exact filenames so the README can reference them:

| Filename | What to capture |
|----------|-----------------|
| `01-landing.png` | The app's landing / empty state before anything is uploaded |
| `02-indexed.png` | After clicking **Process documents** — the "Indexed N chunks" success message |
| `03-answer.png` | A grounded answer (ask *"What is multi-head attention?"*) with the 🔎 Retrieved sources expanded |
| `04-grounded-refusal.png` | The "I don't know" case (ask *"What is the price of Bitcoin?"*) — shows anti-hallucination |

**Tip (Windows):** press `Win + Shift + S` to snip a region, then paste/save it here.
